workflows:
  ios-release:
    name: iOS Release

    environment:
      flutter: stable
      xcode: latest

      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.dftapp.hu

      groups:
        - ios_signing

    scripts:
      - name: Clean
        script: flutter clean

      - name: Install dependencies
        script: flutter pub get

      - name: Install iOS pods
        script: |
          cd ios
          pod install
          cd ..

      - name: Build IPA
        script: |
          xcode-project use-profiles
          flutter build ios --release --no-codesign
          xcode-project build-ipa \
            --workspace ios/Runner.xcworkspace \
            --scheme Runner

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        api_key: $APP_STORE_PRIVATE_KEY
        key_id: Y3R97795M3
        issuer_id: 4eca53a8-970c-4b78-89b4-406c21e7e3cb
        submit_to_testflight: true